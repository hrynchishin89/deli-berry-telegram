# Deli Berry V6 Product Stage

Что внутри:
- кремовый premium Porsche-salon фон;
- товары показываются как объекты на сцене, не как плоские фото;
- stage/cutout assets для тестовых фото;
- поддержка поля `stageImage` в `data/catalog.json`;
- сохранены каталог, корзина, оформление заказа.

Установка: загрузить папки `webapp`, `data`, `docs` в корень GitHub репозитория и нажать Commit changes.
